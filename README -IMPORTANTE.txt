*Es importante que añadan el conector JAR a la libreria en neetbeans
	Viene dentro de mismo proyecto

*Importante importar la base de datos camion a una base de datos que deberas llamar "transporte"  y creas una tabla llamada "camion"
	El script viene en el mismo proyecto

En la clase conexion modificar lo necesario para tener la conexion a tu base de datos "usuario" y "contraseña", por defecto el puerto de mysql es 3306, es el que esta asignado en esta clase, es importante verificar a que puerto lo tienes conectado